<PreferenceScreen>

    <Preference
        android:key="apply_security_changes"
        android:title="Apply Security Changes"
        android:summary="Review and authenticate before applying changes"/>


    <EditTextPreference
        android:key="device_lock_delay"
        android:title="Device Lock Delay"
        android:summary="Delay before device lock"
        android:inputType="number"/>

</PreferenceScreen>
